diff --git a/scribus/scpainterex_cairo.cpp b/scribus/scpainterex_cairo.cpp
--- a/scribus/scpainterex_cairo.cpp
+++ b/scribus/scpainterex_cairo.cpp
@@ -10,6 +10,7 @@ For general Scribus (>=1.3.2) copyright and licensing information please refer
 #include "scimage.h"
 #include "scpattern.h"
 #include "util.h"
+#include "util_math.h"
 
 
 ScPainterEx_Cairo::ScPainterEx_Cairo(cairo_t* context, const QRect& rect, ScribusDoc* doc, bool gray) : ScPainterExBase()
diff --git a/scribus/ui/about.cpp b/scribus/ui/about.cpp
--- a/scribus/ui/about.cpp
+++ b/scribus/ui/about.cpp
@@ -43,7 +43,7 @@ For general Scribus (>=1.3.2) copyright and licensing information please refer
 	#include <podofo/podofo.h>
 #endif
 #ifdef HAVE_POPPLER
-	#include <poppler/cpp/poppler-version.h>
+	#include <poppler-version.h>
 #endif
 #include "scpaths.h"
 #ifdef HAVE_SVNVERSION
diff --git a/scribus/CMakeLists.txt b/scribus/CMakeLists.txt
--- a/scribus/CMakeLists.txt
+++ b/scribus/CMakeLists.txt
@@ -122,6 +122,10 @@ if(HAVE_PODOFO)
 
 endif()
 
+if(HAVE_POPPLER)
+	include_directories(${poppler_INCLUDES})
+endif()
+
 if(HAVE_HYPHEN)
 	target_link_libraries(${EXE_NAME} PRIVATE ${HYPHEN_LIBRARY})
 endif()
@@ -186,6 +190,7 @@ if(WIN32)
 		COMPILE_FLAGS -DCOMPILE_SCRIBUS_MAIN_APP
 		ENABLE_EXPORTS TRUE
 	)
+	target_link_directories(${EXE_NAME} PRIVATE "$<TARGET_LINKER_FILE_DIR:Qt6::Core>")
 	set(WIN32_ONLY_LIBS mscms.lib)
 else()
 	set(WIN32_ONLY_LIBS)
@@ -262,10 +267,6 @@ if(HAIKU)
   target_link_libraries(${EXE_NAME} PRIVATE execinfo)
 endif(HAIKU)
 
-if(WIN32)
-  target_link_libraries(${EXE_NAME} PRIVATE ${QT_QTMAIN_LIBRARY})
-endif()
-
 if(APPLE)
 	target_link_libraries(${EXE_NAME}
 		PUBLIC
diff --git a/scribus/CMakeLists_Sources.txt b/scribus/CMakeLists_Sources.txt
--- a/scribus/CMakeLists_Sources.txt
+++ b/scribus/CMakeLists_Sources.txt
@@ -599,6 +599,7 @@ endif()
 
 if(WIN32)
 	set(SCRIBUS_WIN32_ONLY_SOURCES
+		printpreviewcreator_gdi.cpp
 		scpainterex_cairo.cpp
 		scprintengine_gdi.cpp
 		)
